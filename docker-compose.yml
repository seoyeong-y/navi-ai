version: '3.8'

services:
  ai-server:
    build: .
    container_name: ai-server
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - .:/app
    networks:
      - ai-network

networks:
  ai-network: {}